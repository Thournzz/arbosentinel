package com.arbosentinel.blue.entity.enums;

// ================================================
// BLUE layer — PostgreSQL enum mapping
// Maps to: disease_type PostgreSQL enum (V1)
// ================================================

public enum DiseaseType {
    dengue,
    malaria,
    zika,
    west_nile,
    chikungunya
}
